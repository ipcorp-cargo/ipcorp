spring:
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "http://ipcorpn.com"
            allowedMethods:
              - GET
              - POST
              - DELETE
              - PUT
              - PATCH
springdoc:
  webjars:
    prefix: /api
  swagger-ui:
    enabled: true